POST: http://localhost:5000/api/posts/ => cần đưa vào là body{"title":"","comment":"",url": ""} 
      Authorzition : bearer token accesstoken sau khi đăng nhập 
GET: http://localhost:5000/api/posts/ = > cần truyền vào 
      Authorzition : bearer token accesstoken sau khi đăng nhập 
PUT: http://localhost:5000/api/posts/:id => dùng để cập nhật lại bài posts title bắt buộc
      Authorzition : bearer token accesstoken sau khi đăng nhập => 
      để kiểm tra người dùng có phải là người post và cả quyền sửa hay không
DELETE: http://localhost:5000/api/posts/:id 
      Authorzition : bearer token accesstoken sau khi đăng nhập => 
      để kiểm tra người dùng có phải là người post và cả quyền xóa hay không